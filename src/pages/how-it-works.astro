---
const siteName = "FLY Unlimited";
const contactEmail = "hello@fly-unlimited.com"; // change if needed
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>LiveTime • {siteName}</title>
    <meta name="description" content="LiveTime — how FLY Unlimited works." />
    <style>
      :root{
        --bg:#ffffff;
        --text:#0b0c10;
        --muted:rgba(11,12,16,.68);
        --line:rgba(11,12,16,.10);
        --pill:rgba(11,12,16,.06);
        --max:980px;
        --radius:18px;
      }
      *{box-sizing:border-box}
      html,body{margin:0;padding:0}
      body{
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
        background:var(--bg);
        color:var(--text);
        line-height:1.45;
      }
      a{color:inherit;text-decoration:none}
      a:hover{opacity:.96}

      .wrap{max-width:var(--max);margin:0 auto;padding:22px 20px 72px}

      .top{
        display:flex;
        align-items:center;
        justify-content:space-between;
        gap:16px;
        padding:10px 4px;
      }
      .brand{
        display:flex;
        align-items:center;
        gap:10px;
        min-height:44px;
      }
      .mascot{
        width:26px;height:26px;
        border-radius:10px;
        object-fit:contain;
        display:block;
      }

      /* ✅ icon-only nav (matches homepage) */
      .nav{
        display:flex;
        align-items:center;
        gap:10px;
        padding:8px;
        border:1px solid var(--line);
        border-radius:999px;
        background:rgba(255,255,255,.9);
        backdrop-filter:saturate(180%) blur(8px);
      }
      .navBtn{
        width:40px;height:40px;
        display:grid;
        place-items:center;
        border-radius:999px;
        border:1px solid transparent;
        color:rgba(11,12,16,.72);
      }
      .navBtn:hover{
        background:var(--pill);
        border-color:var(--line);
      }
      .navBtn.active{
        background:#0b0c10;
        color:#fff;
      }
      .navBtn svg{
        width:18px;height:18px;
        stroke:currentColor;
        stroke-width:2.2;
        fill:none;
        stroke-linecap:round;
        stroke-linejoin:round;
      }

      .page{padding:64px 0 0}

      .logoRow{display:flex;justify-content:center;margin-bottom:14px}
      .logoRow img{height:34px;width:auto;display:block}

      h1{
        margin:0 0 10px;
        text-align:center;
        font-size: clamp(28px, 3.2vw, 42px);
        letter-spacing:-.5px;
      }
      .sub{
        margin:0 auto 28px;
        text-align:center;
        max-width:760px;
        color:var(--muted);
        font-size:16px;
      }

      /* ✅ premium cards */
      .cards{display:grid;gap:14px;margin-top:18px}
      .card{
        border:1px solid var(--line);
        border-radius:20px;
        padding:18px;
        background:#fff;
        box-shadow: 0 10px 28px rgba(11,12,16,.04);
      }
      .cardTop{
        display:flex;
        align-items:center;
        justify-content:space-between;
        gap:12px;
        margin-bottom:8px;
      }
      .badge{
        width:40px;height:40px;
        border-radius:14px;
        display:grid;
        place-items:center;
        background:var(--pill);
        border:1px solid var(--line);
        flex:0 0 auto;
      }
      .badge svg{
        width:18px;height:18px;
        stroke:#0b0c10;
        stroke-width:2.2;
        fill:none;
        stroke-linecap:round;
        stroke-linejoin:round;
      }
      .card h3{margin:0;font-size:16px;letter-spacing:-.2px}
      .card p{margin:0;color:var(--muted);font-size:14px;line-height:1.5}

      .footer{
        margin-top:64px;
        padding-top:18px;
        border-top:1px solid var(--line);
        display:flex;
        align-items:center;
        justify-content:space-between;
        gap:14px;
        flex-wrap:wrap;
        color:var(--muted);
        font-size:13px;
      }
      .links{display:flex;gap:14px;flex-wrap:wrap}
      .links a{color:var(--muted)}
      .links a:hover{color:var(--text)}
    </style>
  </head>

  <body>
    <div class="wrap">
      <header class="top">
        <a class="brand" href="/" aria-label="Home">
          <img class="mascot" src="/logo-alt.png" alt="FLY mascot" />
        </a>

        <!-- ✅ icon-only nav -->
        <nav class="nav" aria-label="Navigation">
          <a class="navBtn" href="/" aria-label="Home" title="Home">
            <svg viewBox="0 0 24 24" aria-hidden="true">
              <path d="M3 10.5 12 3l9 7.5" />
              <path d="M5.5 9.8V21h13V9.8" />
            </svg>
          </a>

          <!-- ✅ LiveTime (current page) -->
          <a class="navBtn active" href="/how-it-works/" aria-label="LiveTime" title="LiveTime">
            <svg viewBox="0 0 24 24" aria-hidden="true">
              <path d="M13 2 4 14h7l-1 8 10-14h-7z" />
            </svg>
          </a>

          <a class="navBtn" href="/info/" aria-label="Info" title="Info">
            <svg viewBox="0 0 24 24" aria-hidden="true">
              <circle cx="12" cy="12" r="9" />
              <path d="M12 10.6v6" />
              <path d="M12 7.4h.01" />
            </svg>
          </a>
        </nav>
      </header>

      <main class="page">
        <div class="logoRow">
          <img src="/logo.png" alt="FLY Unlimited" />
        </div>

        <!-- ✅ renamed section -->
        <h1>LiveTime</h1>
        <p class="sub">A simple, polished flow — built for clarity and speed.</p>

        <section class="cards" aria-label="LiveTime steps">
          <div class="card">
            <div class="cardTop">
              <h3>1) Request</h3>
              <span class="badge" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M21 12a8 8 0 0 1-8 8H7l-4 3 1.2-4.7A8 8 0 1 1 21 12z" />
                </svg>
              </span>
            </div>
            <p>Start a request in seconds with clear details.</p>
          </div>

          <div class="card">
            <div class="cardTop">
              <h3>2) Confirm</h3>
              <span class="badge" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M20 6 9 17l-5-5" />
                </svg>
              </span>
            </div>
            <p>Review and confirm — everything stays simple and transparent.</p>
          </div>

          <div class="card">
            <div class="cardTop">
              <h3>3) Complete</h3>
              <span class="badge" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M12 2v4" />
                  <path d="M12 18v4" />
                  <path d="M7 7l3 3" />
                  <path d="M17 7l-3 3" />
                  <circle cx="12" cy="12" r="4" />
                </svg>
              </span>
            </div>
            <p>Finish the flow smoothly. Updates and notifications keep you in sync.</p>
          </div>
        </section>

        <footer class="footer">
          <div>© 2026 {siteName}</div>
          <div class="links">
            <a href="/info/">Contact</a>
            <a href="/terms/">Terms</a>
            <a href="/privacy/">Privacy</a>
          </div>
        </footer>
      </main>
    </div>
  </body>
</html>
